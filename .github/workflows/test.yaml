on:
  push:
    branches:
      - main
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: echo PR number ${{ github.event.pull_request.number }}
        run: echo "${{ github.event.pull_request.number }}"
